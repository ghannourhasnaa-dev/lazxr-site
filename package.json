{
  "name": "lazxr-site",
  "scripts": {
    "watch": "node auto-git.js",
    "promote:preprod": "git checkout preprod && git merge develop --no-ff -m \"merge: develop to preprod\" && git push origin preprod && git checkout develop",
    "promote:main": "git checkout main && git merge preprod --no-ff -m \"merge: preprod to main\" && git push origin main && git checkout develop"
  },
  "dependencies": {
    "chokidar": "^3.6.0"
  }
}